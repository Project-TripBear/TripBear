<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.trip.common.mapper.ReportMapper">

    <insert id="addReport" parameterType="map">
        INSERT INTO tblReports (
            report_id, report_target_type, report_target_id, 
            user_id, reported_user_id, report_reason_type
        ) VALUES (
            seq_reports.NEXTVAL, #{report_target_type}, #{boardSeq}, 
            #{reporterId}, #{reportedUserId}, #{reason}
        )
    </insert>

</mapper>