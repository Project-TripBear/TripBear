<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.trip.admin.car.mapper.AdminCarMapper">

    <insert id="addCar" parameterType="carDTO">
        INSERT INTO tblRentalCar (
            car_id, 
            car_name, 
            car_type, 
            car_fuel_type,
            car_price_per_day, 
            car_image_url,
            
            car_number,  car_seats,   place_Location_id ) VALUES (
            tblRentalCar_SEQ.NEXTVAL, 
            #{carName}, 
            #{carType}, 
            #{carFuel},   #{carPrice},  #{carImage},  #{carNumber},
            #{carSeats},
            #{placeLocationId}
        )
    </insert>
    
    <select id="getMaxPrice" resultType="int">
        SELECT MAX(car_price_per_day) FROM tblRentalCar
    </select>
    
    <select id="getAllCars" parameterType="map" resultType="carDTO">
        SELECT
            car_id AS carId, 
            car_name AS carName, 
            car_type AS carType, 
            car_price_per_day AS carPrice, 
            car_image_url AS carImage, 
            car_fuel_type AS carFuel,
            car_number AS carNumber,
            car_seats AS carSeats,
            place_Location_id AS placeLocationId
        FROM
            tblRentalCar
        WHERE 1=1
        
        <if test="fuelTypes != null and fuelTypes.length > 0">
            AND car_fuel_type IN
            <foreach item="fuel" collection="fuelTypes" open="(" separator="," close=")">
                #{fuel}
            </foreach>
        </if>
        
        <if test="minPrice != null and maxPrice != null">
            AND car_price_per_day BETWEEN #{minPrice} AND #{maxPrice}
        </if>
        
        <choose>
            <when test="sortOrder == 'price_asc'">
                ORDER BY car_price_per_day ASC
            </when>
            <when test="sortOrder == 'price_desc'">
                ORDER BY car_price_per_day DESC
            </when>
            <otherwise>
                ORDER BY car_id DESC
            </otherwise>
        </choose>
    </select>
    
    <select id="selectCarDetail" parameterType="int" resultType="carDTO">
        SELECT
            car_id AS carId, 
            car_name AS carName, 
            car_type AS carType, 
            car_price_per_day AS carPrice, 
            car_image_url AS carImage, 
            car_fuel_type AS carFuel,
            car_number AS carNumber,
            car_seats AS carSeats,
            place_Location_id AS placeLocationId
        FROM
            tblRentalCar
        WHERE
            car_id = #{carId}
    </select>

    <delete id="deleteCar" parameterType="int">
        DELETE FROM tblRentalCar
        WHERE car_id = #{carId}
    </delete>

    <update id="updateCar" parameterType="carDTO">
        UPDATE tblRentalCar
        SET 
            car_name = #{carName},
            car_type = #{carType},
            car_fuel_type = #{carFuel},
            car_price_per_day = #{carPrice},
            car_image_url = #{carImage},
            car_number = #{carNumber},
            car_seats = #{carSeats},
            place_Location_id = #{placeLocationId}
        WHERE 
            car_id = #{carId}
    </update>
</mapper>