<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.trip.board.hotdeal.mapper.HotDealLikeMapper">

	<!-- <resultMap id="noticeResultMap" type="NoticeDTO" >
		<id property="noticePostId" column="notice_post_id"/>
		<result property="adminId" column="admin_id"/>
		<result property="noticeHeader" column="notice_header"/>
		<result property="noticeContent" column="notice_content"/>
		<result property="noticeViewCount" column="notice_view_count"/>
		<result property="noticeRegdate" column="notice_regdate"/>
	</resultMap> -->
	
	 <!-- 총 게시물 수 조회 -->
   	  <select id="likeCheck" resultType="int">
        SELECT COUNT(*) 
        FROM tblHotDealLikes 
        WHERE user_id = #{userSeq} 
        AND hotdeal_id = #{bseq}
    </select>
    
    <insert id="likeAdd">
        INSERT INTO tblHotDealLikes (user_id, hotdeal_id) 
        VALUES (#{userSeq}, #{bseq})
    </insert>
    
    <delete id="likeDel">
        DELETE FROM tblHotDealLikes 
        WHERE user_id = #{userSeq} 
        AND hotdeal_id = #{bseq}
    </delete>
    
    <select id="getLikeCount" resultType="int">
        SELECT COUNT(*) 
        FROM tblHotDealLikes 
        WHERE hotdeal_id = #{bseq}
    </select>
    
    <select id="scrapCheck" resultType="int">
        SELECT COUNT(*) 
        FROM tblHotDealScrap 
        WHERE user_id = #{userSeq} 
        AND hotdeal_id = #{bseq}
    </select>
    
    <insert id="scrapAdd">
        INSERT INTO tblHotDealScrap (hotdeal_scrap_id, user_id, hotdeal_id) 
        VALUES (seqHotDealScrap.nextVal, #{userSeq}, #{bseq})
    </insert>
    
    <delete id="scrapDel">
        DELETE FROM tblHotDealScrap 
        WHERE user_id = #{userSeq} 
        AND hotdeal_id = #{bseq}
    </delete>

	</mapper>
